# Candidate A

Explicit state/event guarded transition-table architecture. JSON Lines runner. It aborts on timeout three, rejects older receive duplicates, and lets only a local outstanding transmission block close.
